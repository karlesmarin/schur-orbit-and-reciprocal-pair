% GENERADO POR gates/P3d_s3_certificados.py --- NO EDITAR A MANO.
\newcommand{\pieCertificados}{$\dagger$: the 7 values of $k$ for which the inequality $A<2^{d}$ does \emph{not} apply at the measured value of $A$; for $k=6$ and $k=10$ it fails by equality. The proof uses only $A\le k$, which is weaker and leaves 29 such values rather than 7; all of those lie inside the certified range too, which is why the finite computation closes the argument rather than postponing it.}
